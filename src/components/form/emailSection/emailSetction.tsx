import React from 'react';
import { Input } from '../inputs/input';

export const EmailSection = () => (
  <section className="email">
    <Input name="Электронная почта" hint="Можно изменить адрес, указанный при регистрации." />
  </section>
);
